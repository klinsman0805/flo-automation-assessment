import { EnergyUsageForm } from '../../../components';

const Page = async () => {
  // Display different step of the form depending on the page params
  return <EnergyUsageForm />;
};

export default Page;
